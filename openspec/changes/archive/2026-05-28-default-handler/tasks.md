# Tasks: Allow More File Formats via Default Handler

## Tasks

- [x] Add `detect_opener()` and `default_handler` to config defaults
- [x] Update `get_extension_handler` to fall back to `default_handler`
- [x] Update `get_template_file` to suppress warning for unconfigured extensions
- [x] Write tests for fallback handler logic
- [x] Write tests for template warning suppression
- [x] Update README and openspec specs
